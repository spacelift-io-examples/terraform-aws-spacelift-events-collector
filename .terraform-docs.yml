formatter: markdown table

output:
  file: "README.md"
  mode: inject
  template: |-
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS -->

sections:
  show:
    - requirements
    - inputs
    - outputs

settings:
  anchor: false
  hide-empty: true
  lockfile: false

sort:
  by: name
  enabled: true
